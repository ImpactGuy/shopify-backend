{
  "name": "bin-it-bright-pdf-generator",
  "version": "1.0.0",
  "description": "Shopify Function to generate PDF labels from order data",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc --outDir dist --module commonjs --target ES2019 --esModuleInterop --allowSyntheticDefaultImports src/index.ts src/server.ts",
    "start": "npm run build && node dist/server.js",
    "test": "node test.js"
  },
  "dependencies": {
    "@shopify/shopify-api": "^11.0.0",
    "dotenv": "^17.2.3",
    "dropbox": "^10.34.0",
    "express": "^4.19.2",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "typescript": "^5.8.3"
  }
}
